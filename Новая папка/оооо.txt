<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <Label Content="Editing Mode:" VerticalAlignment="Center" Margin="0 0 10 0" />
            <ComboBox 
                      SelectedItem="{Binding ElementName=inkCanvas, Path=EditingMode}" />
            <Label Content="Drawing Attributes:" VerticalAlignment="Center" Margin="20 0 10 0" />
            <ListBox 
                     SelectedItem="{Binding ElementName=inkCanvas, Path=DefaultDrawingAttributes}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="{Binding Width}" Height="{Binding Height}" Fill="{Binding Color}" />
                            <TextBlock Text="{Binding Color}" Margin="5,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

        <InkCanvas Grid.Row="1" x:Name="inkCanvas" Background="White" />
    </Grid>
</Window>